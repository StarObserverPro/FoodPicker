# FoodPicker persona art R3

Runtime source: Google Drive `PRODUCTION_R3_LAYERED`.

- `characters/` contains the sixteen approved transparent character cutouts with the deterministic white border.
- `pots/` contains the sixteen matching transparent pot props.
- `backgrounds/` contains the four approved floating-word backgrounds.
- `persona-art.js` is the authoritative archetype-to-layer mapping and composes only the three registered layers.

The R2 composition previews and the small paired WebP contact-sheet crops from PR #9 are not runtime assets. Keep character, pot and background files independently addressable.
